recommendation:
  api:
    host: http://recommend_container  # Docker 컨테이너 서비스 이름
    port: 8000  # FastAPI 컨테이너의 포트

# 로깅 설정
logging:
  level:
    com.zeepseek.backend.domain.recommend: DEBUG
    org.springframework.web.reactive.function.client: DEBUG